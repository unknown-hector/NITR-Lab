Q1) Add two matrices(mxn) using threads, where number of threads is equal to the number of rows(m), each thread will add a single row.

Q2) Add two matrices (mxn) , using mxn threads. Where each thread will two particular cells , one cell each from each matrix.
