1 .Execute each TCP variant and record the avg throughput of each.  
2. Change the TCP variants and the payloadSize for each TCP variant, do a comparative analysis of the TCP variant given in the file wifi-tcp.cc, and generate the result using Gnuplot. 
3. Conclude the result by stating which TCP variant is the best based on your comparison and generate Wireshark result of each TCP.